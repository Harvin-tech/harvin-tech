import { tv } from 'tailwind-variants';

export const appContent = tv({
    base: 'max-w-screen-xl mx-auto px-4 sm:px-6 ',  // removed px-4 since it's in the dynamic classes
    variants: {},
    compoundVariants: [],
});


