import React from 'react';
import StudentDashboard from './(component)/StudentDashboard';

const page = () => {
  return (
    <div>
      <StudentDashboard />
    </div>
  );
};

export default page;
