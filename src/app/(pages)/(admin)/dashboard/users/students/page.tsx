import React from 'react';
import StudentDashboard from './(component)/StudentDashboard';

//[TODO] why this page
const page = () => {
  return (
    <div>
      <StudentDashboard />
    </div>
  );
};

export default page;
