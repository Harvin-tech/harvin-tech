import LeftSidebar from '@/components/dashboard/components/LeftSidebar';
import ProfilePage from '@/components/profile/Profile';
import React from 'react';

const page = () => {
  return <ProfilePage isCreatingUser={false} />;
};

export default page;
